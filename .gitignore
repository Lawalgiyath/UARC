# Dependencies and build output
node_modules
.next
out
build
*.tsbuildinfo

# Local development database, created by `npm run db:start`
.pgdata

# Secrets. Every value the site needs is listed in README.md; the file itself
# is never committed.
.env
.env.*

# Deployment platform state
.vercel

# Handover document: contains live passwords, never commit it.
*-Handover.docx

# Editor and operating system noise
*.log
.DS_Store
Thumbs.db
